# Todo App by Danny Cairns (started with Simple Redux Boilerplate)

####A simple, refresh-persistent todo app build with Redux.

###The easiest way to view the app:

npm install
---
npm run dev
---
localhost:3000
